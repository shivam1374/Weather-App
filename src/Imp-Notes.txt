#Modules
1.express
for opening server so that any one can request and get data

Explore commands/ syntex for express [khud krlena]
const express=require('express')
const A=express()


app.get('',(req,res)=>          //server request accept kr rha hai or query kai hisaab se data send krega e.g " "
{
    res.render('index')
})

1. A.get(first argument,second argument) 

[www.example.com] 
matlab www.example.com kai baad kya likha hai user ne uske hisaab se reply ayega server se 
e.g "     " -> matlab kahli www.example.com hai 
e.g "/help" -> matlab www.example.com/help hai 
e.g "about" -> matlab www.example.com/about hai 
toh jaisa input hoga "iske ander" vaisa hi reply ayega 
toh input kai hisaab se function bnane hai 

second argument-> syntex dekh lo, sbka second argument same hi hai (req,res)
2. A.send(first argument,second argument)

3.console.log(__dirname)  //print current working directory
4.dir_path=path.join(__dirname,'/public') // moving to public folder from to currrent dir.
5.hbs -> used in place of html file with extra feature (dynamicaly run hota hai)
6.{{>headers}}       (loading  content from headers file)

